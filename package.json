{
  "name": "notas",
  "homepage": "https://jarrisoncano.github.io/Tasks/",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "4.6.0",
    "firebase": "8.3.0",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-scripts": "4.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eslint": "standard --fix",
    "deploy": "gh-pages -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "3.1.0",
    "husky": "7.0.1",
    "lint-staged": "11.0.1",
    "standard": "16.0.3"
  },
  "eslintConfig": {
    "extends": "./node_modules/standard/eslintrc.json"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js, jsx}": [
      "eslint --quiet --fix"
    ],
    "*.{json,md,html}": [
      "prettier --write"
    ]
  }
}
